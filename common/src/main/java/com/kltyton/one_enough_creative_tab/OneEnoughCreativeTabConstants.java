package com.kltyton.one_enough_creative_tab;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/** Shared identity and logging constants for the mod. */
public final class OneEnoughCreativeTabConstants {
    /** The mod namespace used by both loaders. */
    public static final String MOD_ID = "one_enough_creative_tab";

    /** The human-readable mod name. */
    public static final String MOD_NAME = "One Enough Creative Tab";

    /** The shared mod logger. */
    public static final Logger LOGGER = LoggerFactory.getLogger(MOD_NAME);

    private OneEnoughCreativeTabConstants() {
    }
}
